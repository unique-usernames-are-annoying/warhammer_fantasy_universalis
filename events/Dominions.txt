
namespace = dominion_events

country_event = {
	id = dominion_events.1
	title = dominion_events.1.t
	desc = dominion_events.1.d
	picture = ARTWORK_eventPicture

	trigger = {
		is_subject_of_type = dominion
		is_at_war = no
		NOT = {
			overlord = {
				OR = {
					truce_with = ROOT
					war_with = ROOT
				}
			}
		}
		OR = {
			liberty_desire = 100

### A lot meaner, but this could happen too

#			AND = {
#				liberty_desire = 50
#				overlord = { is_at_war = yes }
#			}
		}
	}

	is_triggered_only = no
	
	mean_time_to_happen = { months = 180 }
	
### Attack
	option = {
		name = dominion_events.1.option_1
		ai_chance = { factor = 2 }

		add_prestige = 25
		add_legitimacy = 25
		add_republican_tradition = 10
		add_devotion = 25
		add_horde_unity = 25
		add_meritocracy = 25

		overlord = {
			ROOT = {
				declare_war_with_cb = {
					who = PREV
    				casus_belli = cb_independence_war
				}
			}
		}
	}

### Better Not

	option = {
		name = dominion_events.1.option_2
		ai_chance = { factor = 1 }
		
		add_prestige = -25
		add_legitimacy = -25
		add_republican_tradition = -10
		add_devotion = -25
		add_horde_unity = -25
		add_meritocracy = -25

	}
}


### Trade Favors for helper Event
country_event = {
	id = dominion_events.2
	title = dominion_events.2.t
	desc = dominion_events.2.d
	picture = ARTWORK_eventPicture

	trigger = {
		has_subject_of_type = dominion
		any_subject_country = {
			is_subject_of_type = dominion

			NOT = {
				trust = {
					who = ROOT
					value = 100
				}
			}

			owes_favors = { 
			    who = ROOT 
			    value = 100 
			}

		}

	}

	is_triggered_only = no
	hidden = yes
	mean_time_to_happen = { months = 12 }
	
### 
	option = {
		name = dominion_events.2.option_1
		ai_chance = { factor = 100 }

		random_subject_country = {
			limit  = {
				is_subject_of_type = dominion

				NOT = {
					trust = {
						who = ROOT
						value = 100
					}
				}

				owes_favors = { 
				    who = ROOT 
				    value = 100 
				}
			}

			add_favors = {
			   who = ROOT
			   amount = -10
			}

			add_trust = {
			    who = ROOT
			    value = 5
			}
		}
	}

}